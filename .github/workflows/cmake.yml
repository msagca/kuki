name: CMake
on:
  push:
    paths:
      - "**.cpp"
      - "**.c"
      - "**.hpp"
      - "**.h"
      - "**.inl"
      - "**/CMakeLists.txt"
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: "recursive"
      - name: Linux Prerequisites
        if: runner.os == 'Linux'
        run: |
          sudo apt install -y build-essential ninja-build cmake libwayland-dev libxkbcommon-dev xorg-dev
      - name: Windows Prerequisites
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
      - uses: actions/cache@v4
        with:
          path: build
          key: ${{ runner.os }}-cmake-${{ hashFiles('**/CMakeLists.txt') }}
          restore-keys: |
            ${{ runner.os }}-cmake-
      - name: Configure
        run: |
          cmake -G "Ninja" -B build -DBUILD_TESTS=ON .
      - name: Build
        run: |
          cmake --build build
      - uses: actions/upload-artifact@v4
        with:
          name: test-artifacts
          path: build/test
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    needs: build
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: "recursive"
      - uses: actions/download-artifact@v4
        with:
          name: test-artifacts
          path: build/test
      - name: Test
        run: |
          cd build/test
          ./test
